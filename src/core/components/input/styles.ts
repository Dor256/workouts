import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    borderColor: 'gray',
    marginTop: 25,
    height: 40,
    width: 400,
    alignSelf: 'center',
    borderRadius: 15,
    borderWidth: 2,
    padding: 7
  }
});
